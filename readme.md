# Laravel Chat

Laravel chat playing with Laravel Echo and redis.

## Build and run

    $ docker-compose build
    $ docket-compose up -d

## Develop

    $ docker-compose exec php sh
    # su laravel
    $ composer update
    $ php artisan migrate
